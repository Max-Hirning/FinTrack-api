export enum PortfolioErrorMessages {
  findOne = 'No such portfolio',
  findMany = 'No such portfolios',
}

export enum PortfolioSuccessMessages {
  findOne = 'Portfolio was found',
  findMany = 'Portfolios were found',
  removeOne = 'Portfolio was removed',
  updateOne = 'Portfolio was updated',
  createOne = 'Portfolio was created',
  removeMany = 'Portfolios waere removed',
}